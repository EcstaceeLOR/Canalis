{
  "name": "canalis",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "build:web": "npm run build --workspace=@canalis/providers && npm run build --workspace=@canalis/solana && npm run build --workspace=@canalis/core && npm run build --workspace=@canalis/application && npm run build --workspace=@canalis/persistence && npm run build --workspace=@canalis/web",
    "test": "pnpm -r build && pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "db:migrate": "pnpm --filter @canalis/persistence db:migrate",
    "security:check": "node scripts/check-secrets.mjs",
    "ux:check": "node scripts/check-ux-contracts.mjs",
    "public:check": "node scripts/check-public-product.mjs",
    "developer:check": "node scripts/check-developer-platform.mjs",
    "judge:check": "pnpm test && pnpm typecheck && pnpm ux:check && pnpm public:check && pnpm developer:check && pnpm security:check",
    "dev": "pnpm --filter @canalis/web dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
